.table-responsive
  %table#resources.table.table-vcenter.card-table
    %thead
      %tr{role: "row"}
        %th #
        - columns.each do |column|
          %th
            .text
              = resource_class.human_attribute_name(column.to_sym)

    %tbody
      - if local_assigns[:partial]
        = render(partial: partial, collection: resources)
      - else
        = render(resources)
