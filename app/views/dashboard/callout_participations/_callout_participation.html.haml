= content_tag_for(:tr, callout_participation) do
  %td
    = link_to(callout_participation.id, show_location(callout_participation))
  %td
    = link_to(callout_participation.beneficiary_id, dashboard_beneficiary_path(callout_participation.beneficiary_id))
  %td
    = link_to(callout_participation.broadcast_id, dashboard_broadcast_path(callout_participation.broadcast_id))
  %td
    = callout_participation.created_at
