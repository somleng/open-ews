.card-body
  = simple_form_for [:dashboard, resource] do |f|
    = f.input :phone_number, input_html: { autofocus: true }
    .form-group.actions
      = f.button :submit, class: 'btn btn-primary'
      = link_to(translate(:"titles.actions.cancel"), :back, class: 'btn btn-outline-dark')
