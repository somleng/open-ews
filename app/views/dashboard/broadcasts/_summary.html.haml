= show_for(broadcast_summary) do |f|
  = f.attribute :alerts_count
  = f.attribute :alerts_still_to_be_called
  = f.attribute :completed_calls
  = f.attribute :not_answered_calls
  = f.attribute :busy_calls
  = f.attribute :failed_calls
  = f.attribute :errored_calls
